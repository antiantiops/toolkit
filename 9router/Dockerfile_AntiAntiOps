FROM node:22-alpine

ARG ROUTER_VERSION=latest
ARG OPENCLAW_NPM_PACKAGE=

RUN if [ -n "$OPENCLAW_NPM_PACKAGE" ]; then npm install -g "$OPENCLAW_NPM_PACKAGE"; fi

RUN npm install -g 9router@$ROUTER_VERSION 

EXPOSE 20128

ENTRYPOINT ["9router"]
