name: Build and Push 9router Image

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
    inputs:
      router_version:
        description: "9router version (leave empty for default)"
        required: false
        type: string
      openclaw_npm_package:
        description: "OpenClaw npm package spec (e.g. scope/pkg@1.2.3); leave empty to skip install"
        required: false
        type: string
  # schedule:
  #   - cron: "17 2 * * *"

env:
  IMAGE_NAME: antiantiops/9router
  DEFAULT_ROUTER_VERSION: "0.4.11"     # <--- EDIT DEFAULT 9ROUTER VERSION HERE https://www.npmjs.com/package/9router
  DEFAULT_OPENCLAW_NPM_PACKAGE: ""      # <--- Empty means install `openclaw@latest`

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout toolkit repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Determine 9router version
        id: router_version
        run: |
          TARGET_VERSION="${{ inputs.router_version || env.DEFAULT_ROUTER_VERSION }}"
          if [ "$TARGET_VERSION" = "latest" ]; then
            VERSION=$(npm show 9router version)
          else
            VERSION="$TARGET_VERSION"
          fi
          echo "version=$VERSION" >> $GITHUB_ENV

      - name: Determine OpenClaw npm package
        run: |
          TARGET_OPENCLAW_PACKAGE="${{ inputs.openclaw_npm_package || env.DEFAULT_OPENCLAW_NPM_PACKAGE }}"
          if [ -z "$TARGET_OPENCLAW_PACKAGE" ]; then
            TARGET_OPENCLAW_PACKAGE="openclaw@latest"
          fi
          echo "openclaw_npm_package=$TARGET_OPENCLAW_PACKAGE" >> $GITHUB_ENV

      - name: Extract Docker metadata 
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ env.version }}-{{sha}}
            type=raw,value=${{ env.version }}
            type=raw,value=latest

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./9router
          file: ./9router/Dockerfile_AntiAntiOps
          build-args: |
            ROUTER_VERSION=${{ env.version }}
            OPENCLAW_NPM_PACKAGE=${{ env.openclaw_npm_package }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=9router
          cache-to: type=gha,mode=max,scope=9router
